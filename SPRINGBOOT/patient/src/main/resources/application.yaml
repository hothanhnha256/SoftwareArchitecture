server:
  port: 8900
  servlet:
    context-path: /patient
spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/localhost"
    driverClassName: "org.postgresql.Driver"
    username: postgres
    password: nhahothanh2564.

  jpa:

    hibernate:
      ddl-auto: update
    show-sql: true

app:
  services:
    patient: http://localhost:8900/patient


#  security:
#    user:
#      name: admin
#      password: admin